_schema-version: 3.3.0
ID: travelRequest
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: travelRequest-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: travelRequest-auth
      - name: travelRequest-db
      - name: travelRequest-destination

  - name: travelRequest-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: travelRequest-db
  - name: travelRequest
    type: approuter.nodejs
    path: app/router
    parameters:
      keep-existing-routes: true
      disk-quota: 256M
      memory: 256M
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true
      - name: travelRequest-auth
      - name: travelRequest-destination
      - name: travelRequest-html5-runtime
    provides:
      - name: app-api
        properties:
          app-protocol: ${protocol}
          app-uri: ${default-uri}

  - name: travelRequest-app-deployer
    type: com.sap.application.content
    path: gen
    build-parameters:
      build-result: app/
      requires:
        - name: travelRequestemployee
          artifacts:
            - employee.zip
          target-path: app/
        - name: travelRequestmanager
          artifacts:
            - manager.zip
          target-path: app/
    requires:
      - name: travelRequest-auth
      - name: srv-api
      - name: travelRequest-html5-repo-host
        parameters:
          content-target: true
          config:
            HTML5Runtime_enabled: true
    parameters:
      config:
        destinations:
          - Name: srv-api
            URL: ~{srv-api/srv-url}
            Authentication: NoAuthentication
            Type: HTTP
            ProxyType: Internet
            HTML5.ForwardAuthToken: true
            HTML5.DynamicDestination: true

  - name: travelRequestemployee
    type: html5
    path: app/employee
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []
  - name: travelRequestmanager
    type: html5
    path: app/manager
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: travelRequest-destinations
    type: com.sap.application.content
    requires:
      - name: travelRequest-auth
        parameters:
          service-key:
            name: travelRequest-auth-key
      - name: travelRequest-html5-repo-host
        parameters:
          service-key:
            name: travelRequest-html5-repo-host-key
      - name: srv-api
      - name: travelRequest-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: travelRequest-html5-repository
              ServiceInstanceName: travelRequest-html5-repo-host
              ServiceKeyName: travelRequest-html5-repo-host-key
              sap.cloud.service: travelRequest.service
            - Name: travelRequest-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: travelRequest-auth
              ServiceKeyName: travelRequest-auth-key
              sap.cloud.service: travelRequest.service

resources:
  - name: travelRequest-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: travelRequest-${org}-${space}
        tenant-mode: dedicated
        oauth2-configuration:
          credential-types:
            - "binding-secret"
            - "x509"
          redirect-uris:
            - https://*~{app-api/app-uri}/**
        role-collections:
          - name: 'Employee (travelRequest ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Employee'
          - name: 'Manager (travelRequest ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Manager'
          - name: 'Admin (travelRequest ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.Admin'
    requires:
      - name: app-api
  - name: travelRequest-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: travelRequest-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
    requires:
      - name: srv-api

  - name: travelRequest-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: travelRequest-html5-runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
